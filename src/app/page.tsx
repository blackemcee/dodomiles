export default function Home() {
  return (
    <main className="flex-1 flex items-center justify-center px-4 py-16">
      <div className="max-w-xl text-center">
        <p className="text-sm uppercase tracking-[0.18em] text-[color:var(--faint)] mb-4">
          DodoMiles
        </p>
        <h1 className="text-4xl sm:text-5xl font-semibold leading-tight text-[color:var(--ink)] mb-4">
          Trail running trips, in good company.
        </h1>
        <p className="text-lg text-[color:var(--muted)]">
          The new DodoMiles site is being rebuilt. The trips gallery lands
          here shortly.
        </p>
      </div>
    </main>
  );
}
